reference:
        "{{reference}}"
name:
        "{{name}}"

min_perc:
        "{{minperc}}"

now:
        "{{now}}"

mask:
        "{{mask}}"

day:
        "{{day}}"

isolates:
        "{{isolates}}"